# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env & secrets — never commit
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# 本地大体积资产（阶段 0 下载 / 阶段 1 生成，可再生产物不进 git）
models/
data/

# 历史遗留：旧版 staging 组装产物（现行流程无此目录，可整目录删除）
staging/

# 训练产物（本地验证输出 + 云端下载的产物）
outputs/

*.log

# codegraph 工具索引（本地生成，不入库）
.codegraph/
